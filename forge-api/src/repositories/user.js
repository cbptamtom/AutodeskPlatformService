const login = async ({ email, password }) => {
	console.log("login user repository");
};

const register = ({ name, email, password, address, phoneNumber, jobTitle }) => {
	console.log("register user repository");
};
export default { login, register };
